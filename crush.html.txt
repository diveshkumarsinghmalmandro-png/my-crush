<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Someone Special</title>
    <style>
        :root {
            --primary-pink: #ff4d6d;
            --soft-bg: #fff0f3;
        }

        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: var(--soft-bg);
            font-family: 'Segoe UI', cursive, sans-serif;
            overflow: hidden;
        }

        /* Floating Hearts Animation */
        .heart {
            position: absolute;
            color: var(--primary-pink);
            font-size: 20px;
            user-select: none;
            animation: float 4s linear forwards;
            z-index: 0;
        }

        @keyframes float {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
        }

        .card {
            background: white;
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 15px 35px rgba(255, 77, 109, 0.2);
            text-align: center;
            z-index: 10;
            max-width: 400px;
            width: 90%;
            border: 2px solid #ffb3c1;
        }

        h1 {
            color: #c9184a;
            font-size: 1.8rem;
            min-height: 60px;
        }

        p {
            color: #590d22;
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .buttons {
            margin-top: 30px;
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        button {
            padding: 12px 30px;
            border-radius: 50px;
            border: none;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            font-size: 1rem;
        }

        #yesBtn {
            background-color: var(--primary-pink);
            color: white;
            box-shadow: 0 4px 15px rgba(255, 77, 109, 0.4);
        }

        #noBtn {
            background-color: #dee2e6;
            color: #495057;
            position: relative;
        }

        .success-content {
            display: none;
        }
    </style>
</head>
<body>

    <div class="card" id="mainCard">
        <div id="askSection">
            <h1 id="typedText"></h1>
            <p>I've been thinking about this for a while, and I just had to let you know...</p>
            <div class="buttons">
                <button id="yesBtn" onclick="showLove()">Yes</button>
                <button id="noBtn" onmouseover="moveNo()">No</button>
            </div>
        </div>

        <div id="successSection" class="success-content">
            <h1 style="font-size: 3rem;">❤️</h1>
            <h2>I knew you'd say yes!</h2>
            <p>You just made me the happiest person alive. Let's make something beautiful together.</p>
        </div>
    </div>

    <script>
        // Typewriter Effect
        const text = "I have a crush on you... will you be mine?";
        let i = 0;
        function typeWriter() {
            if (i < text.length) {
                document.getElementById("typedText").innerHTML += text.charAt(i);
                i++;
                setTimeout(typeWriter, 100);
            }
        }
        typeWriter();

        // Floating Hearts Logic
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = (Math.random() * 3 + 2) + 's';
            document.body.appendChild(heart);
            setTimeout(() => heart.remove(), 4000);
        }
        setInterval(createHeart, 300);

        // Evil "No" Button
        function moveNo() {
            const btn = document.getElementById('noBtn');
            const x = Math.